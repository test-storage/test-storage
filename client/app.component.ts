import { Component } from '@angular/core';

@Component({
  //  moduleId: module.id,
    selector: 'ts-root',
    template: `
        <router-outlet></router-outlet>
        `
})
export class AppComponent { }
