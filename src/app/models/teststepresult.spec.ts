import { TestStepResult } from './teststepresult.model';

describe('TestStepResult', () => {
    it('should create an instance', () => {
        expect(new TestStepResult()).toBeTruthy();
    });
});
