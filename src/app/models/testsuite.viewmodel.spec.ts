import { TestsuiteViewModel } from './testsuite.viewmodel';

describe('TestsuiteViewModel', () => {
    it('should create an instance', () => {
        expect(new TestsuiteViewModel()).toBeTruthy();
    });
});
